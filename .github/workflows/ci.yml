name: build

on: [push, pull_request]

jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set Up OpenGL
      run: sudo apt-get install libgl1-mesa-dev libudev-dev
    - name: Configure CMake
      run: mkdir build && cd build && cmake ..
    - name: Build project
      run: cmake --build build --target chip8_emu
    - name: Run tests
      run: cmake --build build --target tests

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Configure CMake
        run: mkdir build && cd build && cmake ..
      - name: Build project
        run: cmake --build build --target chip8_emu
      - name: Run tests
        run: cmake --build build --target tests
